import React from 'react';
import {Switch, Route} from "react-router-dom";

const AppMain = () => {
    return (
        <div>
            <Switch>
                <Route exact></Route>
            </Switch>
        </div>
    );
};

export default AppMain;